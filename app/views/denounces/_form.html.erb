<%= form_with(model: denounce) do |form| %>
  <% if denounce.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(denounce.errors.count, "error") %> prohibited this denounce from being saved:</h2>

      <ul>
        <% denounce.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :desc, style: "display: block" %>
    <%= form.text_field :desc %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
